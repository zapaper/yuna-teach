# next-auth@5 (beta) declares an optional peer on nodemailer@^7 but
# we run nodemailer@^8 (no actual code conflict — the optional path
# isn't used). npm 7+ refuses the install on peer mismatch by
# default; flip to legacy resolution so CI ('npm ci') matches what
# we install locally.
legacy-peer-deps=true
